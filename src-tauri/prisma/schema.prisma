datasource db {
    provider = "sqlite"
    url      = "file:database.db"
}

generator client {
    provider = "cargo prisma"
    output   = "../src/prisma.rs"
}

model User {
    id          String @id @default(cuid())
    displayName String
}
